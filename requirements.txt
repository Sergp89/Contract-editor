# =============================================================================
# 🎨 My Python UI App — Dependencies
# Фреймворк: Flet (Material Design 3 + анимации)
# Версия файла: 1.0.0
# Обновлён: 2026-04
# =============================================================================

# -----------------------------------------------------------------------------
# 🔷 Основное: UI-фреймворк
# -----------------------------------------------------------------------------
flet>=0.25.0,<0.26.0

# -----------------------------------------------------------------------------
# 🔷 Код-стайл и линтинг (обязательно для качества)
# -----------------------------------------------------------------------------
ruff>=0.4.0,<0.5.0          # Быстрый линтер + автофикс (заменяет flake8, isort)
black>=24.0.0,<25.0.0       # Форматирование кода
isort>=5.13.0,<6.0.0        # Сортировка импортов (опционально, если не используете ruff)

# -----------------------------------------------------------------------------
# 🔷 Тестирование
# -----------------------------------------------------------------------------
pytest>=8.0.0,<9.0.0        # Фреймворк для тестов
pytest-asyncio>=0.23.0,<0.24.0  # Поддержка async-тестов для Flet
pytest-cov>=4.1.0,<5.0.0    # Покрытие кода тестами

# -----------------------------------------------------------------------------
# 🔷 Разработка и отладка
# -----------------------------------------------------------------------------
python-dotenv>=1.0.0,<2.0.0 # Загрузка переменных из .env файла
watchdog>=4.0.0,<5.0.0      # Hot-reload при разработке (опционально)
rich>=13.0.0,<14.0.0        # Красивые логи в консоли (опционально)

# -----------------------------------------------------------------------------
# 🔷 Типизация и статический анализ (опционально, но рекомендуется)
# -----------------------------------------------------------------------------
mypy>=1.8.0,<2.0.0          # Проверка типов
types-python-dateutil>=2.8.0  # Стабы для dateutil (если используется)

# -----------------------------------------------------------------------------
# 🔷 Сборка и публикация (если планируете pip install)
# -----------------------------------------------------------------------------
build>=1.0.0,<2.0.0         # Сборка wheel/sdist
twine>=4.0.0,<5.0.0         # Публикация в PyPI (опционально)

# -----------------------------------------------------------------------------
# 🔷 Пре-коммит хуки (для автоматизации качества)
# -----------------------------------------------------------------------------
pre-commit>=3.6.0,<4.0.0    # Git hooks для авто-линта перед коммитом